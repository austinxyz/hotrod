# Dev Log — 2026-07-18

## pickup-confirmation: explore → propose → apply → archive (full opsx cycle)

First end-to-end run of the OpenSpec + Superpowers × Signadot integration on
hotrod-opsx. Change `pickup-confirmation` (capability `dispatch-tracking`)
shipped and archived in one day.

### What shipped

- `services/driver/dispatchstore.go` — Redis-backed dispatch records
  (`dispatch:<requestID>`, 5-min TTL, Store/Get/UpdateStatus)
- `services/driver/arrived.go` — `POST /dispatches/{requestID}/arrived`
  (404 unknown / idempotent 200 / transition + `req-<id>-arrived` notification)
- Consumer writes the record after best-ETA selection; k8s `driver` Service (8082)
- 8 unit tests (miniredis, first service-level tests in the repo); TDD RED→GREEN
  throughout, both eval groups passed attempt 1 (98/100, 99/100)

### Signadot integration — what actually worked

- Official skills installed from signadot/agent-skills (`signadot-plan`,
  `signadot-validate`); opsx propose/apply wired to invoke them
- Plan authored against the real `signadot plan schema` (action DAG, actionIDs),
  not the integration spec's guessed `kind: TestPlan` — spec needs updating
- 2.V VALIDATE: sandboxed driver fork (image `hotrod-pickup:dev` via
  `ctr images import` into kind) + plan `lkyjcsgkqkjxn` run `dp4c62nm2suys`:
  5/5 assertions green on austin-staging-1. Evaluator consumed the verdict as
  Runtime evidence (the core integration mechanic, proven)
- Smoke test: baseline temporarily on fork image; UI Logs panel showed the full
  chain ending in "Driver T772466C arrived at pickup"; baseline restored after

### Friction log (fed into CLAUDE.md Pitfalls + sync-back)

1. Windows: no signadot CLI binary → docker wrapper (`~\bin\signadot`);
   `auth login` keyring fails in container → config.yaml with org + api_key
2. Plans need Managed Plan Runners enabled in the dashboard per cluster;
   `jobrunnergroup` is not a plan runner
3. Org image allowlist rejected every run-container/k6 image even after
   allowlisting (bug, to report to Joe) → plan rebuilt from actionbox-exempt
   request-http/eval/check; async wait via ordered step chain + httpbin delay
4. OpenSpec CLI doesn't substitute `{{date}}/{{change}}` in outputPath →
   literal-named shim files needed for artifact detection (bug, to report)
5. Frontend requestID restarts per browser session → `dispatch:<requestID>`
   collides across sessions; needs globally unique id in a real system
   (input for driver-status brainstorm)

### Next

- `/opsx:explore driver-status` (brainstorm first — DRAFT spec)
- Sync skill changes back to opsx-superpowers; update integration spec with the
  real plan schema + Joe bug reports
